framework:
    messenger:
        failure_transport: failed

        transports:
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                retry_strategy:
                    max_retries: 3
                    multiplier: 2
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'

        routing:
            # Messages implementing it are handled by the worker (messenger:consume async).
            App\Message\AsyncMessageInterface: async

when@test:
    framework:
        messenger:
            transports:
                async: 'sync://'
